import React from 'react';
import { Typography, Box } from '@mui/material';

const ProfilePage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>個人資訊</Typography>
    <Typography>這裡是個人資訊頁面 Placeholder。</Typography>
  </Box>
);

export default ProfilePage;
