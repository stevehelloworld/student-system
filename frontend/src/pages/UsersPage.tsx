import React from 'react';
import { Typography, Box } from '@mui/material';

const UsersPage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>學生管理（Admin/Teacher）</Typography>
    <Typography>這裡是學生管理頁面 Placeholder。</Typography>
  </Box>
);

export default UsersPage;
