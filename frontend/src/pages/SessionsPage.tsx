import React from 'react';
import { Typography, Box } from '@mui/material';

const SessionsPage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>課程場次管理</Typography>
    <Typography>這裡是課程場次頁面 Placeholder。</Typography>
  </Box>
);

export default SessionsPage;
